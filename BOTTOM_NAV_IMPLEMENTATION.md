# Bottom Navigation 구현 계획서

> SSUP 앱에 Bottom Navigation Tabs 도입 및 구조 개선

---

## 📋 개요

### 목표

- 5개의 Bottom Navigation 탭 도입 (홈, 채팅, 포스트 작성, 알림, 프로필)
- 기존 기능들을 탭 구조로 이전
- 채팅 탭은 Coming Soon 페이지로 placeholder 처리
- 모바일 친화적인 slick & trendy 디자인

### 확정 사항

| 항목            | 결정                                                         |
| --------------- | ------------------------------------------------------------ |
| 탭 개수         | 5개 (홈, 채팅, 포스트, 알림, 프로필)                         |
| 채팅 탭         | Coming Soon 페이지                                           |
| 탭 바 표시 범위 | 메인 페이지만 (세부 페이지에서 숨김)                         |
| 비로그인 처리   | 로그인 필요 탭 클릭 시 로그인 페이지로 리다이렉트            |
| 탭 디자인       | 아이콘만 (라벨 없음)                                         |
| 헤더            | SSUP 로고 + 로그인 버튼(비로그인 시) + 뷰토글/필터(홈에서만) |

### 탭 구성

| 순서 | 탭          | 아이콘 | 경로             | 로그인 필요 |
| ---- | ----------- | ------ | ---------------- | ----------- |
| 1    | 홈          | 🏠     | `/`              | ❌          |
| 2    | 채팅        | 💬     | `/chat`          | ✅          |
| 3    | 포스트 작성 | ➕     | `/post/new`      | ✅          |
| 4    | 알림        | 🔔     | `/notifications` | ✅          |
| 5    | 프로필      | 👤     | `/profile`       | ✅          |

### 탭 바 표시/숨김 페이지

| 페이지                            | 탭 바 표시 |
| --------------------------------- | ---------- |
| `/` (홈)                          | ✅         |
| `/chat` (채팅 - Coming Soon)      | ✅         |
| `/notifications` (알림)           | ✅         |
| `/profile` (프로필)               | ✅         |
| `/profile/posts` (내 포스트)      | ✅         |
| `/post/[id]` (포스트 상세)        | ❌         |
| `/post/new` (포스트 작성)         | ❌         |
| `/profile/edit` (프로필 수정)     | ❌         |
| `/auth/*` (로그인/회원가입)       | ❌         |
| `/user/[id]` (다른 사용자 프로필) | ❌         |

---

## 🏗️ Phase 1: Bottom Navigation 컴포넌트 구현

### Step 1.1: BottomNavigation 컴포넌트 생성

**커밋 메시지:** `feat: create BottomNavigation component`

**파일:**

- `src/components/layout/BottomNavigation.tsx`

**구현 내용:**

```typescript
// 컴포넌트 구조
interface BottomNavigationProps {
  // props 없음 - 내부에서 usePathname, useAuth 사용
}

// 탭 아이템 구조
interface NavItem {
  icon: React.ReactNode;
  href: string;
  requiresAuth: boolean;
  badge?: number; // 알림 탭용
}
```

**기능:**

- 5개 탭 아이콘 렌더링
- 현재 경로에 따른 활성 탭 하이라이트
- 알림 탭에 배지 표시 (읽지 않은 알림 수)
- iOS Safe Area 대응 (`pb-safe`)
- 부드러운 hover/active 애니메이션

**스타일:**

- 배경: 흰색 (`bg-white`)
- 상단 테두리: 얇은 회색 (`border-t border-gray-100`)
- 아이콘 크기: 24px
- 활성 아이콘: 파란색 (`text-blue-600`)
- 비활성 아이콘: 회색 (`text-gray-400`)
- 높이: 약 56px + safe area

---

### Step 1.2: 인증 기반 네비게이션 로직

**커밋 메시지:** `feat: add auth-based navigation logic to BottomNavigation`

**구현 내용:**

- `useAuth` 훅으로 로그인 상태 확인
- 비로그인 시 로그인 필요 탭 클릭 → `/auth/login` 리다이렉트
- 리다이렉트 시 `returnUrl` 쿼리 파라미터 전달 (로그인 후 원래 가려던 페이지로)

**로직:**

```typescript
const handleNavClick = (item: NavItem, e: React.MouseEvent) => {
  if (item.requiresAuth && !user) {
    e.preventDefault();
    router.push(`/auth/login?returnUrl=${encodeURIComponent(item.href)}`);
  }
};
```

---

### Step 1.3: 알림 배지 연동

**커밋 메시지:** `feat: integrate notification badge with BottomNavigation`

**구현 내용:**

- 기존 `NotificationBadge` 컴포넌트 로직 활용 또는 새 훅 생성
- 읽지 않은 알림 수를 알림 탭 아이콘 위에 표시
- 99개 초과 시 "99+" 표시

---

## 🏗️ Phase 2: 레이아웃 구조 변경

### Step 2.1: MainLayout 컴포넌트 생성

**커밋 메시지:** `feat: create MainLayout component with BottomNavigation`

**파일:**

- `src/components/layout/MainLayout.tsx`

**구현 내용:**

```typescript
interface MainLayoutProps {
  children: React.ReactNode;
  showBottomNav?: boolean; // 기본값 true
}
```

**기능:**

- BottomNavigation 조건부 렌더링
- 컨텐츠 영역에 bottom padding 추가 (탭 바 높이만큼)
- 탭 바가 없는 페이지에서는 padding 없음

---

### Step 2.2: 헤더 컴포넌트 리팩토링

**커밋 메시지:** `refactor: simplify Header component`

**파일:**

- `src/components/layout/Header.tsx` (신규)

**변경 내용:**

- 기존: SSUP 로고 + 알림 배지 + 프로필 아이콘
- 변경: SSUP 로고 + 로그인 버튼(비로그인 시만)

**구현:**

```typescript
interface HeaderProps {
  showLoginButton?: boolean; // 비로그인 사용자에게만 표시
}
```

---

### Step 2.3: 홈페이지 레이아웃 적용

**커밋 메시지:** `refactor: apply new layout to home page`

**파일:**

- `src/app/page.tsx`

**변경 내용:**

- 헤더에서 알림 배지, 프로필 아이콘 제거
- FAB (새 포스트 버튼) 제거
- 뷰 토글/정렬 옵션은 유지
- MainLayout 적용

---

### Step 2.4: 알림 페이지 레이아웃 적용

**커밋 메시지:** `refactor: apply new layout to notifications page`

**파일:**

- `src/app/notifications/page.tsx`

**변경 내용:**

- MainLayout 적용 (showBottomNav: true)
- 헤더 단순화

---

### Step 2.5: 프로필 페이지 레이아웃 적용

**커밋 메시지:** `refactor: apply new layout to profile page`

**파일:**

- `src/app/profile/page.tsx`
- `src/app/profile/posts/page.tsx`

**변경 내용:**

- MainLayout 적용 (showBottomNav: true)

---

### Step 2.6: 세부 페이지 레이아웃 적용

**커밋 메시지:** `refactor: hide BottomNavigation on detail pages`

**파일:**

- `src/app/post/[id]/page.tsx`
- `src/app/post/new/page.tsx`
- `src/app/profile/edit/page.tsx`
- `src/app/user/[id]/page.tsx`

**변경 내용:**

- MainLayout 적용 (showBottomNav: false)
- 뒤로가기 버튼 확인/추가

---

## 🏗️ Phase 3: Coming Soon 페이지 (채팅)

### Step 3.1: Coming Soon 페이지 생성

**커밋 메시지:** `feat: create chat coming soon page`

**파일:**

- `src/app/chat/page.tsx`

**구현 내용:**

- 채팅 아이콘 (크게)
- "Coming Soon" 또는 "Chat is on the way!" 메시지
- 간단한 설명 텍스트
- 홈으로 돌아가기 버튼 (선택사항)
- MainLayout 적용 (showBottomNav: true)

**디자인:**

- 중앙 정렬
- 부드러운 애니메이션 (선택사항)
- 브랜드 컬러 사용

---

## 🏗️ Phase 4: 로그인 플로우 개선

### Step 4.1: 로그인 후 리다이렉트 처리

**커밋 메시지:** `feat: handle returnUrl redirect after login`

**파일:**

- `src/app/auth/login/page.tsx`
- `src/components/auth/LoginForm.tsx`

**구현 내용:**

- URL의 `returnUrl` 쿼리 파라미터 읽기
- 로그인 성공 시 해당 URL로 리다이렉트
- `returnUrl` 없으면 홈(`/`)으로 리다이렉트

---

## 🏗️ Phase 5: 정리 및 최적화

### Step 5.1: 불필요한 코드 제거

**커밋 메시지:** `chore: remove deprecated navigation elements`

**변경 내용:**

- 홈페이지의 FAB 버튼 코드 제거
- 헤더의 알림/프로필 아이콘 관련 코드 정리
- 사용하지 않는 import 제거

---

### Step 5.2: 상수 및 라우트 정리

**커밋 메시지:** `chore: update ROUTES constants`

**파일:**

- `src/lib/constants.ts`

**변경 내용:**

- `ROUTES.CHAT` 추가
- 기존 라우트 확인 및 정리

---

### Step 5.3: 컴포넌트 export 정리

**커밋 메시지:** `chore: organize layout component exports`

**파일:**

- `src/components/layout/index.ts` (신규)

**내용:**

```typescript
export { BottomNavigation } from "./BottomNavigation";
export { MainLayout } from "./MainLayout";
export { Header } from "./Header";
```

---

## 🧪 Test Cases

### TC-1: Bottom Navigation 표시

| ID     | 케이스                               | 예상 결과                   |
| ------ | ------------------------------------ | --------------------------- |
| TC-1.1 | 홈(`/`)에서 Bottom Nav 확인          | 5개 탭이 화면 하단에 표시됨 |
| TC-1.2 | 알림(`/notifications`)에서 확인      | 5개 탭 표시됨               |
| TC-1.3 | 프로필(`/profile`)에서 확인          | 5개 탭 표시됨               |
| TC-1.4 | 내 포스트(`/profile/posts`)에서 확인 | 5개 탭 표시됨               |
| TC-1.5 | 채팅(`/chat`)에서 확인               | 5개 탭 표시됨               |

### TC-2: Bottom Navigation 숨김

| ID     | 케이스                                    | 예상 결과       |
| ------ | ----------------------------------------- | --------------- |
| TC-2.1 | 포스트 상세(`/post/[id]`)에서 확인        | Bottom Nav 없음 |
| TC-2.2 | 포스트 작성(`/post/new`)에서 확인         | Bottom Nav 없음 |
| TC-2.3 | 프로필 수정(`/profile/edit`)에서 확인     | Bottom Nav 없음 |
| TC-2.4 | 로그인(`/auth/login`)에서 확인            | Bottom Nav 없음 |
| TC-2.5 | 회원가입(`/auth/signup`)에서 확인         | Bottom Nav 없음 |
| TC-2.6 | 다른 사용자 프로필(`/user/[id]`)에서 확인 | Bottom Nav 없음 |

### TC-3: 탭 활성 상태

| ID     | 케이스                           | 예상 결과     |
| ------ | -------------------------------- | ------------- |
| TC-3.1 | 홈에서 홈 탭 아이콘 색상         | 파란색 (활성) |
| TC-3.2 | 홈에서 다른 탭 아이콘 색상       | 회색 (비활성) |
| TC-3.3 | 알림에서 알림 탭 아이콘 색상     | 파란색 (활성) |
| TC-3.4 | 프로필에서 프로필 탭 아이콘 색상 | 파란색 (활성) |
| TC-3.5 | 채팅에서 채팅 탭 아이콘 색상     | 파란색 (활성) |

### TC-4: 로그인된 사용자 네비게이션

| ID     | 케이스              | 예상 결과                             |
| ------ | ------------------- | ------------------------------------- |
| TC-4.1 | 홈 탭 클릭          | `/`로 이동                            |
| TC-4.2 | 채팅 탭 클릭        | `/chat`으로 이동 (Coming Soon 페이지) |
| TC-4.3 | 포스트 작성 탭 클릭 | `/post/new`로 이동                    |
| TC-4.4 | 알림 탭 클릭        | `/notifications`로 이동               |
| TC-4.5 | 프로필 탭 클릭      | `/profile`로 이동                     |

### TC-5: 비로그인 사용자 네비게이션

| ID     | 케이스              | 예상 결과                                             |
| ------ | ------------------- | ----------------------------------------------------- |
| TC-5.1 | 홈 탭 클릭          | `/`로 이동 (정상 작동)                                |
| TC-5.2 | 채팅 탭 클릭        | `/auth/login?returnUrl=%2Fchat`로 리다이렉트          |
| TC-5.3 | 포스트 작성 탭 클릭 | `/auth/login?returnUrl=%2Fpost%2Fnew`로 리다이렉트    |
| TC-5.4 | 알림 탭 클릭        | `/auth/login?returnUrl=%2Fnotifications`로 리다이렉트 |
| TC-5.5 | 프로필 탭 클릭      | `/auth/login?returnUrl=%2Fprofile`로 리다이렉트       |

### TC-6: 로그인 후 리다이렉트

| ID     | 케이스                                                   | 예상 결과               |
| ------ | -------------------------------------------------------- | ----------------------- |
| TC-6.1 | `/auth/login?returnUrl=%2Fchat`에서 로그인 성공          | `/chat`으로 이동        |
| TC-6.2 | `/auth/login?returnUrl=%2Fnotifications`에서 로그인 성공 | `/notifications`로 이동 |
| TC-6.3 | `/auth/login` (returnUrl 없음)에서 로그인 성공           | `/`로 이동              |

### TC-7: 알림 배지

| ID     | 케이스                    | 예상 결과                 |
| ------ | ------------------------- | ------------------------- |
| TC-7.1 | 읽지 않은 알림 0개        | 배지 표시 안됨            |
| TC-7.2 | 읽지 않은 알림 5개        | 알림 탭에 "5" 배지 표시   |
| TC-7.3 | 읽지 않은 알림 99개       | 알림 탭에 "99" 배지 표시  |
| TC-7.4 | 읽지 않은 알림 100개 이상 | 알림 탭에 "99+" 배지 표시 |

### TC-8: 헤더

| ID     | 케이스                    | 예상 결과                                  |
| ------ | ------------------------- | ------------------------------------------ |
| TC-8.1 | 비로그인 사용자 - 홈 헤더 | SSUP 로고 + "Log in" 버튼 표시             |
| TC-8.2 | 로그인 사용자 - 홈 헤더   | SSUP 로고만 표시 (알림/프로필 아이콘 없음) |
| TC-8.3 | 홈 헤더 아래              | 정렬 드롭다운 + 뷰 토글 표시               |

### TC-9: Coming Soon 페이지 (채팅)

| ID     | 케이스                         | 예상 결과                      |
| ------ | ------------------------------ | ------------------------------ |
| TC-9.1 | `/chat` 페이지 접속 (로그인됨) | Coming Soon 메시지 표시        |
| TC-9.2 | Coming Soon 페이지 UI          | 채팅 아이콘 + 안내 메시지 표시 |
| TC-9.3 | Coming Soon 페이지 Bottom Nav  | 5개 탭 표시, 채팅 탭 활성화    |

### TC-10: 세부 페이지 뒤로가기

| ID      | 케이스                   | 예상 결과          |
| ------- | ------------------------ | ------------------ |
| TC-10.1 | 포스트 상세에서 뒤로가기 | 이전 페이지로 이동 |
| TC-10.2 | 포스트 작성에서 취소     | 이전 페이지로 이동 |
| TC-10.3 | 프로필 수정에서 뒤로가기 | `/profile`로 이동  |

### TC-11: Safe Area 대응

| ID      | 케이스                  | 예상 결과                     |
| ------- | ----------------------- | ----------------------------- |
| TC-11.1 | iPhone (노치 있는 기기) | 하단 safe area 만큼 추가 패딩 |
| TC-11.2 | 일반 기기               | 기본 높이로 표시              |

### TC-12: 기존 기능 제거 확인

| ID      | 케이스                 | 예상 결과     |
| ------- | ---------------------- | ------------- |
| TC-12.1 | 홈에서 FAB 버튼        | 존재하지 않음 |
| TC-12.2 | 헤더에서 알림 아이콘   | 존재하지 않음 |
| TC-12.3 | 헤더에서 프로필 아이콘 | 존재하지 않음 |

---

## 📁 파일 변경 요약

### 신규 파일

- `src/components/layout/BottomNavigation.tsx`
- `src/components/layout/MainLayout.tsx`
- `src/components/layout/Header.tsx`
- `src/components/layout/index.ts`
- `src/app/chat/page.tsx`

### 수정 파일

- `src/app/page.tsx` - 헤더 단순화, FAB 제거, 레이아웃 적용
- `src/app/notifications/page.tsx` - 레이아웃 적용
- `src/app/profile/page.tsx` - 레이아웃 적용
- `src/app/profile/posts/page.tsx` - 레이아웃 적용
- `src/app/post/[id]/page.tsx` - 레이아웃 적용 (showBottomNav: false)
- `src/app/post/new/page.tsx` - 레이아웃 적용 (showBottomNav: false)
- `src/app/profile/edit/page.tsx` - 레이아웃 적용 (showBottomNav: false)
- `src/app/user/[id]/page.tsx` - 레이아웃 적용 (showBottomNav: false)
- `src/app/auth/login/page.tsx` - returnUrl 처리
- `src/components/auth/LoginForm.tsx` - returnUrl 리다이렉트
- `src/lib/constants.ts` - ROUTES.CHAT 추가

---

## ⏱️ 예상 소요 시간

| Phase    | 설명                       | 예상 시간   |
| -------- | -------------------------- | ----------- |
| Phase 1  | Bottom Navigation 컴포넌트 | 2-3시간     |
| Phase 2  | 레이아웃 구조 변경         | 2-3시간     |
| Phase 3  | Coming Soon 페이지         | 30분-1시간  |
| Phase 4  | 로그인 플로우 개선         | 1시간       |
| Phase 5  | 정리 및 최적화             | 30분-1시간  |
| **총계** |                            | **6-9시간** |

---

## 📝 참고사항

### 디자인 참고

- Instagram, Twitter/X 모바일 앱의 Bottom Navigation
- 아이콘: Heroicons (outline 스타일)
- 활성 상태: filled 또는 색상 변경으로 구분

### 추후 확장

- 채팅 기능 구현 시 `/chat` 페이지 교체
- 채팅 탭에 읽지 않은 메시지 배지 추가
- 애니메이션 효과 추가 (페이지 전환, 탭 전환)

---

_작성일: 2026-01-04_
_버전: 1.0_
